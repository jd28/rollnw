# Preview isolated world context

The first play preview uses a transient creature registered in the existing kernel while keeping it out of authored area membership, undo state, persistence, scripts, encounters, and lifecycle events. Revisit a separate world context only when implementing runtime systems demonstrates concrete shared-state interference that cannot be removed more cheaply.
