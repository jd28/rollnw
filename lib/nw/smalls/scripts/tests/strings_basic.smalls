from core.test import { test, reset, summary };

[[test("tests.strings_basic corpus")]]
fn corpus() {
    reset();

    var a = "hello";
    var b = "world";
    test("string_eq", a == "hello");
    test("string_ne", a != b);

    summary();
}
