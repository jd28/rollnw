from core.types import { ResRef };

// == Propsets ================================================================
// ============================================================================

[[propset(Store)]]
type StoreState {
    on_closed: ResRef;
    on_opened: ResRef;
    blackmarket_markdown: int;
    identify_price: int;
    markdown: int;
    markup: int;
    max_price: int;
    gold: int;
    blackmarket: int;
};
