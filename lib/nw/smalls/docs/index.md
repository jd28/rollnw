# Smalls Docs

Smalls is the active rules/script authoring path in rollNW. Older docs may focus on NWScript parsing or C++ rule modifiers; those systems still exist, but Smalls is where new language, embedding, and authored RPG toolset/game rules work should be expected to land.

- `spec.md` - language spec
