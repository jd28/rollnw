# Note, this is not good

MAIN:
  - $struct_id: 1
    ID: byte
    LIST:
      - $struct_id: 1
        CR: float
        FACTION: string
        ID: byte
        LIST:
          - $struct_id: 1
            NAME: string
            RESREF: resref
            STRREF: dword
        NAME: string
        RESREF: resref
        STRREF: dword
    STRREF: dword
