import core.math as M;
import core.string as S;

fn main() {
    var v1: M.Vector = M.Vector { 1.0, 2.0, 3.0 };
    var v2: M.Vector = M.Vector { 4.0, 5.0, 6.0 };
    var d = M.dot(v1, v2);
    assert(d > 0.0);

    var s = S.to_upper(S.trim("  fuzz  "));
    println(s);
}
